[pytest]
testpaths = .
addopts = -v --tb=short
markers =
    rt_ratio(ratio): scale firmware simulated time by ratio relative to wall time. 0.1 = 10x faster, 2.0 = half speed. See launch_native_sim_firmware().
