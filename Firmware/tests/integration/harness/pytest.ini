[pytest]
testpaths = .
addopts = -v --tb=short
markers =
    rt_ratio(ratio): scale firmware simulated time by ratio relative to wall time. 10.0 = 10x faster, 0.1 = 10x slower. See launch_native_sim_firmware().
