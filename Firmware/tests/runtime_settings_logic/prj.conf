CONFIG_ZTEST=y
CONFIG_LOG=y

# Settings subsystem present so SETTINGS_STATIC_HANDLER_DEFINE compiles, but
# the save/load/init entry points are --wrap'd to test stubs (see CMakeLists +
# main.c). No flash backend — this suite tests the logic layer, not real NVS.
CONFIG_SETTINGS=y
CONFIG_SETTINGS_RUNTIME=y
