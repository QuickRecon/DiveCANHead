# Pull in the board-local solenoid driver's Kconfig (defines CONFIG_SOLENOID
# and its init-priority / log-level / current-check symbols) so this test
# builds the real driver under test.
rsource "../../drivers/solenoid/Kconfig"

source "Kconfig.zephyr"
