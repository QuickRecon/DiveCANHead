# hw_version.c reads tri-state detect GPIOs and compares against the DT
# expected-pattern. The gpio-sim controller (auto-enabled by its DT node)
# preserves externally-driven values across pull reconfigures so the
# floating / high / low decode arms can all be exercised.
CONFIG_ZTEST=y
CONFIG_GPIO=y
CONFIG_LOG=y
